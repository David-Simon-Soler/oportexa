import Link from "next/link";
import { formatDate, formatMoney, statusLabel } from "../lib/format";
import type { GrantSummary } from "../lib/db/types";
export function GrantCard({ grant }: { grant: GrantSummary }) { return <article className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex flex-wrap items-center gap-2 text-xs text-slate-500"><span className={grant.isOpen ? "rounded-full bg-emerald-50 px-2.5 py-1 font-medium text-emerald-700" : "rounded-full bg-slate-100 px-2.5 py-1 font-medium text-slate-600"}>{grant.isOpen ? "Abierta" : "Cerrada / no confirmada"}</span><span>BDNS {grant.bdnsCode}</span></div><h2 className="mt-3 text-lg font-semibold leading-snug text-slate-950"><Link href={`/subvenciones/${grant.slug}`} className="hover:text-amber-700">{grant.title ?? "Convocatoria sin título"}</Link></h2><dl className="mt-4 grid gap-3 text-sm text-slate-600 sm:grid-cols-3"><div><dt className="text-xs uppercase tracking-wide text-slate-400">Importe</dt><dd className="mt-1 font-medium text-slate-800">{formatMoney(grant.totalBudget)}</dd></div><div><dt className="text-xs uppercase tracking-wide text-slate-400">Cierre</dt><dd className="mt-1 font-medium text-slate-800">{grant.applicationEndDate ? formatDate(grant.applicationEndDate) : grant.isOpen ? "Sin fecha de cierre" : "No especificado"}</dd></div><div><dt className="text-xs uppercase tracking-wide text-slate-400">Organismo</dt><dd className="mt-1 font-medium text-slate-800">{grant.organizations.map((item) => item.label).join(", ") || "Organismo no especificado"}</dd></div></dl><p className="mt-4 text-xs text-slate-500">{statusLabel(grant.isOpen, grant.applicationEndDate)}</p></article>; }
