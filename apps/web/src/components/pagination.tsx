import Link from "next/link";
export function Pagination({ page, total, pageSize, query }: { page: number; total: number; pageSize: number; query: URLSearchParams }) { const pages = Math.ceil(total / pageSize); if (pages <= 1) return null; const href = (n: number) => { const next = new URLSearchParams(query); next.set("page", String(n)); return `/subvenciones?${next.toString()}`; }; return <nav aria-label="Paginación" className="flex items-center justify-between border-t border-slate-200 pt-5 text-sm"><span className="text-slate-500">Página {page} de {pages}</span><div className="flex gap-2">{page > 1 && <Link className="rounded-md border border-slate-300 px-3 py-2 hover:bg-slate-50" href={href(page - 1)}>Anterior</Link>}{page < pages && <Link className="rounded-md border border-slate-300 px-3 py-2 hover:bg-slate-50" href={href(page + 1)}>Siguiente</Link>}</div></nav>; }
